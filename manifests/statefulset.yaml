apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: broker
spec:
  selector:
    matchLabels:
      app: broker
  serviceName: "broker"
  replicas: 3
  template:
    metadata:
      labels:
        app: broker
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: broker
        image: banders2/dreamstream-broker
        ports:
        - containerPort: 80
          name: broker